#include "main.h"
#include <stdio.h>

/**
 * main - prints its name
 * @argc: Command lind argument count
 * @argv: Command line array
 *
 * Return: Always zero
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
